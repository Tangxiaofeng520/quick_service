#pragma once
#include "Msg.h"
#include <queue>
class service
{
private:
    /* data */
public:
    //queue< >
        struct message_queue * message_queue
    service(/* args */);
    ~service();
};

service::service(/* args */)
{
}

service::~service()
{
}
